# 项目资源框架说明

本说明记录当前资源分区的职责边界，帮助后续开发保持文件位置一致。

## 资源分区

- 源码资源位于 `src/stefan_app/`，按应用入口、界面、模型、求解、绘图、导出和工具模块划分。
- 静态资源位于 `resources/`，用于存放样式、图标和示例参数。
- 测试资源位于 `tests/`，用于放置可自动执行的项目验证代码。
- 开发配置位于项目根目录和 `.vscode/`，用于固定环境、依赖和编辑器行为。

## 维护要求

- 新增资源应优先放入现有分区。
- 新增模块应保持职责单一，避免跨层混放。
- 中文文档统一使用 UTF-8 编码。
